apiVersion: apps/v1
kind: Deployment
metadata:
  name: vol-monitor
  namespace: deribit
  labels:
    app: vol-monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vol-monitor
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: vol-monitor
    spec:
      restartPolicy: Always
      # nodeSelector removed - multi-arch image supports both amd64 and arm64
      containers:
      - name: vol-monitor
        image: crpi-ck06yio90i1ttwlz.cn-beijing.personal.cr.aliyuncs.com/n_common/vol-monitor:latest
        imagePullPolicy: Always
        workingDir: /etc/vol-monitor
        args:
          - "--config"
          - "config.toml"
        volumeMounts:
        - name: config
          mountPath: /etc/vol-monitor
          readOnly: true
        env:
        - name: RUST_LOG
          value: "info"
        - name: HTTPS_PROXY
          value: "http://192.168.2.98:8890"
      volumes:
      - name: config
        configMap:
          name: vol-monitor-config
          items:
          - key: config.toml
            path: config.toml
          defaultMode: 0644
