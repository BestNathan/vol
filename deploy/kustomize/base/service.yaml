apiVersion: v1
kind: Service
metadata:
  name: agent-server
  labels:
    app.kubernetes.io/part-of: vol-agent
spec:
  selector:
    app.kubernetes.io/part-of: vol-agent
  ports:
    - name: ws
      protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
