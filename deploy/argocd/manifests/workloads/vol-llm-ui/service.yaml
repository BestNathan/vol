apiVersion: v1
kind: Service
metadata:
  name: vol-llm-ui
  namespace: vol-agent-system
  labels:
    app.kubernetes.io/name: vol-llm-ui
    app.kubernetes.io/part-of: vol-agent
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: vol-llm-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
