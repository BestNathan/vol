apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-server
  namespace: vol-agent-system
  labels:
    app.kubernetes.io/name: agent-server
    app.kubernetes.io/part-of: vol-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent-server
        app.kubernetes.io/part-of: vol-agent
    spec:
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/arch: arm64
      imagePullSecrets:
        - name: ghcr-bestnathan
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: agent-server
          image: ghcr.io/bestnathan/vol-agent-server:cp-6461475
          imagePullPolicy: IfNotPresent
          workingDir: /app
          args:
            - "--config"
            - "/etc/agent-server/agent-server.toml"
          ports:
            - containerPort: 3001
              name: ws
              protocol: TCP
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          volumeMounts:
            - name: config
              mountPath: /etc/agent-server
              readOnly: true
            - name: agent-definitions
              mountPath: /app/.agents/agents
              readOnly: true
            - name: agent-providers
              mountPath: /app/.agents/providers
              readOnly: true
            - name: agent-skills
              mountPath: /app/.agents/skills
              readOnly: true
            - name: agent-sandboxes
              mountPath: /app/.agents/sandboxes
              readOnly: true
            - name: mcp-config
              mountPath: /app/.mcp.json
              subPath: mcp.json
              readOnly: true
            - name: data
              mountPath: /app/data
            - name: tmp
              mountPath: /tmp
            - name: ssh-key
              mountPath: /app/.ssh
              readOnly: true
          env:
            - name: OTEL_SERVICE_NAME
              value: "agent-server-cp"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: ANTHROPIC_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agent-provider-secrets
                  key: ANTHROPIC_AUTH_TOKEN
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-provider-secrets
                  key: OPENAI_API_KEY
            - name: HTTPS_PROXY
              value: "http://192.168.2.98:8890"
            - name: HTTP_PROXY
              value: "http://192.168.2.98:8890"
            - name: NO_PROXY
              value: "localhost,127.0.0.1,192.168.0.0/16,10.0.0.0/8,kubernetes.default.svc,.svc.cluster.local"
            - name: RUST_LOG
              value: "info"
      volumes:
        - name: config
          configMap:
            name: agent-server-config
            items:
              - key: agent-server.toml
                path: agent-server.toml
            defaultMode: 0644
        - name: agent-definitions
          projected:
            sources:
              - configMap: { name: agent-def-ansible }
              - configMap: { name: agent-def-dingtalk }
              - configMap: { name: agent-def-explore }
              - configMap: { name: agent-def-general-purpose }
              - configMap: { name: agent-def-review }
              - configMap: { name: agent-def-ssh-test }
              - configMap: { name: agent-def-task-cli-test }
            defaultMode: 0644
        - name: agent-providers
          projected:
            sources:
              - configMap: { name: provider-anthropic-dashscope }
            defaultMode: 0644
        - name: agent-skills
          projected:
            sources:
              - configMap:
                  name: skill-clarifying-requirements
                  items:
                    - key: SKILL.md
                      path: clarifying-requirements/SKILL.md
            defaultMode: 0644
        - name: agent-sandboxes
          projected:
            sources:
              - configMap: { name: sandbox-ansible-prod }
              - configMap: { name: sandbox-local-for-cli }
              - configMap: { name: sandbox-ssh-dev }
            defaultMode: 0644
        - name: mcp-config
          configMap:
            name: mcp-config
            defaultMode: 0644
        - name: data
          emptyDir:
            sizeLimit: 1Gi
        - name: tmp
          emptyDir: {}
        - name: ssh-key
          secret:
            secretName: ansible-ssh-key
            defaultMode: 0644
