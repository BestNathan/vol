apiVersion: v1
kind: Secret
metadata:
  name: ansible-ssh-key
  namespace: vol-agent-system
  labels:
    app.kubernetes.io/name: ansible-ssh-key
    app.kubernetes.io/part-of: vol-agent
    app.kubernetes.io/component: runtime-config
type: Opaque
stringData:
  # Replace with actual SSH private key content before applying
  id_ed25519: |
    PLACEHOLDER — replace with your actual SSH private key
