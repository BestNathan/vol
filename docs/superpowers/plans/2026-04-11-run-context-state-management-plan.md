# RunContext 状态管理重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Agent 内部状态管理迁移到 RunContext，使 `run()` 方法直接从 RunContext 构造并返回 `AgentResponse`。

**Architecture:** 在 `RunContext` 上新增状态收集字段和方法，移除 agent loop 中的局部状态收集器，使用 `finalize()` 方法构造 `AgentResponse`。

**Tech Stack:** Tokio async runtime, Arc<RwLock> for thread-safe state mutation, chrono for timestamps

---

## 文件结构

| 文件 | 变更类型 | 职责 |
|------|----------|------|
| `crates/vol-llm-agent/src/react/state.rs` | 创建 | 新增状态类型 `ReasoningStep`、`ToolCallRecord` |
| `crates/vol-llm-agent/src/react/run_context.rs` | 修改 | 新增状态字段和方法 |
| `crates/vol-llm-agent/src/react/response.rs` | 修改 | 更新 `AgentResponse.reasoning` 类型 |
| `crates/vol-llm-agent/src/react/agent.rs` | 修改 | 使用 RunContext 方法记录状态，调用 `finalize()` |
| `crates/vol-llm-agent/src/react/mod.rs` | 修改 | 导出 `state` 模块 |
| `crates/vol-llm-agent/src/lib.rs` | 修改 | 导出新类型 |
| 测试和示例文件 | 修改 | 适配新 API |

---

## Task 1: 创建 state 模块和类型

**Files:**
- Create: `crates/vol-llm-agent/src/react/state.rs`
- Modify: `crates/vol-llm-agent/src/react/mod.rs`

- [ ] **Step 1: 创建 state.rs 文件**

创建文件 `crates/vol-llm-agent/src/react/state.rs`：

```rust
//! Agent internal state types.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

/// A single reasoning step in the agent's thought chain
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReasoningStep {
    /// Iteration number (1-based)
    pub iteration: u32,
    
    /// The thinking content generated by LLM
    pub thinking: String,
    
    /// Timestamp when this step was recorded
    pub timestamp: DateTime<Utc>,
    
    /// Duration of LLM call in milliseconds (if available)
    pub duration_ms: Option<u64>,
}

impl ReasoningStep {
    pub fn new(iteration: u32, thinking: String, duration_ms: Option<u64>) -> Self {
        Self {
            iteration,
            thinking,
            timestamp: Utc::now(),
            duration_ms,
        }
    }
}

/// Record of a single tool call during agent execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ToolCallRecord {
    pub tool_name: String,
    pub arguments: String,
    pub result: String,
    pub iteration: u32,
    pub success: bool,
}
```

- [ ] **Step 2: 更新 mod.rs 导出 state 模块**

修改 `crates/vol-llm-agent/src/react/mod.rs`，添加：

```rust
pub mod state;
```

- [ ] **Step 3: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully with no errors

- [ ] **Step 4: 提交**

```bash
git add crates/vol-llm-agent/src/react/state.rs crates/vol-llm-agent/src/react/mod.rs
git commit -m "feat: create state module with ReasoningStep and ToolCallRecord"
```

---

## Task 2: 更新 RunContext 新增状态字段

**Files:**
- Modify: `crates/vol-llm-agent/src/react/run_context.rs`

- [ ] **Step 1: 添加 use 语句**

在 `run_context.rs` 顶部添加：

```rust
use super::state::{ReasoningStep, ToolCallRecord};
```

- [ ] **Step 2: 在 RunContext 结构体中新增字段**

在 `RunContext` 结构体中添加（在 `plugin_event_tx` 字段之后）：

```rust
    // Internal state collection
    pub(crate) reasoning_chain: Arc<RwLock<Vec<ReasoningStep>>>,
    pub(crate) tool_call_records: Arc<RwLock<Vec<ToolCallRecord>>>,
    pub(crate) final_content: Arc<RwLock<Option<String>>>,
    pub(crate) error: Arc<RwLock<Option<String>>>,
```

- [ ] **Step 3: 更新 RunContext::new() 初始化新增字段**

在 `RunContext::new()` 的 `ctx = Self { ... }` 中添加：

```rust
    reasoning_chain: Arc::new(RwLock::new(Vec::new())),
    tool_call_records: Arc::new(RwLock::new(Vec::new())),
    final_content: Arc::new(RwLock::new(None)),
    error: Arc::new(RwLock::new(None)),
```

- [ ] **Step 4: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully with no errors

- [ ] **Step 5: 提交**

```bash
git add crates/vol-llm-agent/src/react/run_context.rs
git commit -m "feat: add state collection fields to RunContext"
```

---

## Task 3: 实现 RunContext 状态记录方法

**Files:**
- Modify: `crates/vol-llm-agent/src/react/run_context.rs`

- [ ] **Step 1: 实现 record_reasoning_step 方法**

在 `impl RunContext` 中添加：

```rust
/// Record a reasoning step
pub async fn record_reasoning_step(&self, thinking: String, duration_ms: Option<u64>) {
    let iteration = self.iteration.load(std::sync::atomic::Ordering::SeqCst);
    let step = ReasoningStep::new(iteration, thinking, duration_ms);
    self.reasoning_chain.write().await.push(step);
}
```

- [ ] **Step 2: 实现 record_tool_call 方法**

```rust
/// Record a tool call
pub async fn record_tool_call(&self, record: ToolCallRecord) {
    self.tool_call_records.write().await.push(record);
}
```

- [ ] **Step 3: 实现 set_final_content 方法**

```rust
/// Set final answer content
pub async fn set_final_content(&self, content: String) {
    *self.final_content.write().await = Some(content);
}
```

- [ ] **Step 4: 实现 set_error 方法**

```rust
/// Set error information
pub async fn set_error(&self, error: String) {
    *self.error.write().await = Some(error);
}
```

- [ ] **Step 5: 实现 get 方法**

```rust
/// Get a clone of the reasoning chain
pub async fn get_reasoning_chain(&self) -> Vec<ReasoningStep> {
    self.reasoning_chain.read().await.clone()
}

/// Get a clone of tool call records
pub async fn get_tool_call_records(&self) -> Vec<ToolCallRecord> {
    self.tool_call_records.read().await.clone()
}
```

- [ ] **Step 6: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully

- [ ] **Step 7: 提交**

```bash
git add crates/vol-llm-agent/src/react/run_context.rs
git commit -m "feat: implement RunContext state recording methods"
```

---

## Task 4: 实现 RunContext::finalize() 方法

**Files:**
- Modify: `crates/vol-llm-agent/src/react/run_context.rs`

- [ ] **Step 1: 添加 futures 依赖（如需要）**

检查 `crates/vol-llm-agent/Cargo.toml`，如果没有 futures：

```toml
[dependencies]
futures = "0.3"
```

- [ ] **Step 2: 实现 finalize() 方法**

在 `impl RunContext` 中添加：

```rust
/// Build AgentResponse from collected state
pub fn finalize(&self) -> AgentResponse {
    use futures::executor::block_on;
    
    let reasoning_chain = block_on(self.reasoning_chain.read());
    let tool_call_records = block_on(self.tool_call_records.read());
    let final_content = block_on(self.final_content.read());
    let error = block_on(self.error.read());
    
    AgentResponse {
        content: final_content.clone().unwrap_or_default(),
        reasoning: reasoning_chain.clone(),
        run_id: self.run_id.clone(),
        session_id: self.session_id.clone(),
        iterations: self.iteration.load(std::sync::atomic::Ordering::SeqCst),
        tool_calls: tool_call_records.clone(),
        error: error.clone(),
    }
}
```

- [ ] **Step 3: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully

- [ ] **Step 4: 提交**

```bash
git add crates/vol-llm-agent/src/react/run_context.rs
git commit -m "feat: implement RunContext::finalize() method"
```

---

## Task 5: 更新 AgentResponse 类型

**Files:**
- Modify: `crates/vol-llm-agent/src/react/response.rs`

- [ ] **Step 1: 更新 use 语句**

修改导入：

```rust
use crate::react::state::{ReasoningStep, ToolCallRecord};
```

- [ ] **Step 2: 更新 AgentResponse 结构体**

将 `reasoning` 字段从 `String` 改为 `Vec<ReasoningStep>`：

```rust
/// Agent response with full execution context
#[derive(Debug, Clone, Serialize)]
pub struct AgentResponse {
    /// Final answer content
    pub content: String,

    /// Complete reasoning chain (all thinking steps)
    pub reasoning: Vec<ReasoningStep>,

    /// Execution metadata
    pub run_id: String,
    pub session_id: String,
    pub iterations: u32,

    /// All tool calls made during execution
    pub tool_calls: Vec<ToolCallRecord>,

    /// Error information if any tool call failed
    pub error: Option<String>,
}
```

- [ ] **Step 3: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: May have errors in files that construct AgentResponse (expected, will fix in later tasks)

- [ ] **Step 4: 提交**

```bash
git add crates/vol-llm-agent/src/react/response.rs
git commit -m "refactor: change AgentResponse.reasoning from String to Vec<ReasoningStep>"
```

---

## Task 6: 更新 agent.rs 使用 RunContext 记录状态

**Files:**
- Modify: `crates/vol-llm-agent/src/react/agent.rs`

- [ ] **Step 1: 添加 use 语句**

在文件顶部添加：

```rust
use crate::react::state::ToolCallRecord;
```

- [ ] **Step 2: 删除局部变量声明**

在 `agent_task` 开始处，删除：

```rust
// 删除这些行
let mut reasoning_chain = Vec::new();
let mut tool_call_records = Vec::new();
let mut final_content = String::new();
let mut final_iterations = 0u32;
```

- [ ] **Step 3: 更新 LLM stream 消费后的思考记录**

找到：

```rust
// Collect reasoning chain
if !thinking.is_empty() {
    reasoning_chain.push(format!("[Iteration {}] {}", iteration, thinking));
}
```

改为：

```rust
// Record reasoning step
if !thinking.is_empty() {
    run_ctx.record_reasoning_step(thinking, None).await;
}
```

- [ ] **Step 4: 更新工具调用记录**

找到成功工具调用记录：

```rust
// Record successful tool call
tool_call_records.push(crate::react::response::ToolCallRecord {
    tool_name: call.name.clone(),
    arguments: call.arguments.clone(),
    result: result.content.clone(),
    iteration,
    success: true,
});
```

改为：

```rust
// Record tool call
run_ctx.record_tool_call(ToolCallRecord {
    tool_name: call.name.clone(),
    arguments: call.arguments.clone(),
    result: result.content.clone(),
    iteration,
    success: true,
}).await;
```

- [ ] **Step 5: 更新错误工具调用记录**

找到错误处理：

```rust
// Record failed tool call
tool_call_records.push(crate::react::response::ToolCallRecord {
    tool_name: call.name.clone(),
    arguments: call.arguments.clone(),
    result: format!("Error: {}", e),
    iteration,
    success: false,
});

return Err(crate::AgentError::ToolExecution {
    tool: call.name.clone(),
    error: e.to_string(),
});
```

改为：

```rust
// Record failed tool call
run_ctx.record_tool_call(ToolCallRecord {
    tool_name: call.name.clone(),
    arguments: call.arguments.clone(),
    result: format!("Error: {}", e),
    iteration,
    success: false,
}).await;

// Set error in RunContext
run_ctx.set_error(format!("Tool execution failed: {}", e)).await;

return Err(crate::AgentError::ToolExecution {
    tool: call.name.clone(),
    error: e.to_string(),
});
```

- [ ] **Step 6: 更新最终回答存储**

找到：

```rust
// Store final response data
let final_content = content.clone();
let final_iterations = iteration;
```

改为：

```rust
// Store final response data
run_ctx.set_final_content(content.clone()).await;
```

- [ ] **Step 7: 更新 AgentComplete 事件构造**

找到：

```rust
// === Emit AgentComplete ===
let response = AgentResponse {
    content: final_content.clone(),
    reasoning: reasoning_chain.join("\n\n"),
    run_id: run_id_clone.clone(),
    session_id: session_id.clone(),
    iterations: final_iterations,
    tool_calls: tool_call_records.clone(),
    error: None,
};
```

改为：

```rust
// === Emit AgentComplete ===
let response = run_ctx.finalize();
```

- [ ] **Step 8: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: May have errors in tests/examples that construct AgentResponse

- [ ] **Step 9: 提交**

```bash
git add crates/vol-llm-agent/src/react/agent.rs
git commit -m "refactor: use RunContext methods for state collection in agent loop"
```

---

## Task 7: 更新模块导出

**Files:**
- Modify: `crates/vol-llm-agent/src/lib.rs`

- [ ] **Step 1: 更新 lib.rs 导出**

在 `src/lib.rs` 中添加：

```rust
pub use react::state::{ReasoningStep, ToolCallRecord};
```

- [ ] **Step 2: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully (library only)

- [ ] **Step 3: 提交**

```bash
git add crates/vol-llm-agent/src/lib.rs
git commit -m "chore: export ReasoningStep and ToolCallRecord from crate root"
```

---

## Task 8: 更新测试文件

**Files:**
- Modify: `crates/vol-llm-agent/src/react/run_context.rs` (tests)
- Modify: `crates/vol-llm-agent/src/observability/plugin.rs` (tests)
- Modify: `crates/vol-llm-agent/src/plugins/caching.rs` (tests)

- [ ] **Step 1: 添加 RunContext 状态方法单元测试**

在 `run_context.rs` 的 `#[cfg(test)]` 模块中添加：

```rust
#[tokio::test]
async fn test_record_reasoning_step() {
    let (ctx, _rx) = RunContext::new(
        "test-run".to_string(),
        "test input".to_string(),
        "session-1".to_string(),
        Arc::new(Session::new(
            "session-1".to_string(),
            Arc::new(InMemorySessionStore::new()),
            Arc::new(InMemoryMessageStore::new()),
        )),
        Arc::new(vol_llm_tool::ToolRegistry::new()),
        AgentConfig::default(),
    );

    ctx.record_reasoning_step("First thought".to_string(), Some(100)).await;
    ctx.record_reasoning_step("Second thought".to_string(), None).await;

    let chain = ctx.get_reasoning_chain().await;
    assert_eq!(chain.len(), 2);
    assert_eq!(chain[0].thinking, "First thought");
    assert_eq!(chain[0].duration_ms, Some(100));
    assert_eq!(chain[1].thinking, "Second thought");
}

#[tokio::test]
async fn test_record_tool_call() {
    let (ctx, _rx) = RunContext::new(
        "test-run".to_string(),
        "test".to_string(),
        "session-1".to_string(),
        Arc::new(Session::new(
            "session-1".to_string(),
            Arc::new(InMemorySessionStore::new()),
            Arc::new(InMemoryMessageStore::new()),
        )),
        Arc::new(vol_llm_tool::ToolRegistry::new()),
        AgentConfig::default(),
    );

    let record = ToolCallRecord {
        tool_name: "test_tool".to_string(),
        arguments: "{}".to_string(),
        result: "result".to_string(),
        iteration: 1,
        success: true,
    };
    ctx.record_tool_call(record).await;

    let records = ctx.get_tool_call_records().await;
    assert_eq!(records.len(), 1);
    assert_eq!(records[0].tool_name, "test_tool");
}

#[tokio::test]
async fn test_set_final_content() {
    let (ctx, _rx) = RunContext::new(
        "test-run".to_string(),
        "test".to_string(),
        "session-1".to_string(),
        Arc::new(Session::new(
            "session-1".to_string(),
            Arc::new(InMemorySessionStore::new()),
            Arc::new(InMemoryMessageStore::new()),
        )),
        Arc::new(vol_llm_tool::ToolRegistry::new()),
        AgentConfig::default(),
    );

    ctx.set_final_content("Final answer".to_string()).await;
    
    // Use finalize to verify
    let response = ctx.finalize();
    assert_eq!(response.content, "Final answer");
}

#[tokio::test]
async fn test_finalize() {
    let (ctx, _rx) = RunContext::new(
        "test-run".to_string(),
        "test".to_string(),
        "session-1".to_string(),
        Arc::new(Session::new(
            "session-1".to_string(),
            Arc::new(InMemorySessionStore::new()),
            Arc::new(InMemoryMessageStore::new()),
        )),
        Arc::new(vol_llm_tool::ToolRegistry::new()),
        AgentConfig::default(),
    );

    ctx.record_reasoning_step("thought".to_string(), None).await;
    ctx.set_final_content("answer".to_string()).await;

    let response = ctx.finalize();
    
    assert_eq!(response.content, "answer");
    assert_eq!(response.reasoning.len(), 1);
    assert_eq!(response.reasoning[0].thinking, "thought");
    assert_eq!(response.run_id, "test-run");
    assert_eq!(response.session_id, "session-1");
}
```

- [ ] **Step 2: 更新 observability/plugin.rs 测试中的 AgentResponse 构造**

找到：

```rust
AgentStreamEvent::AgentComplete { response: AgentResponse { content: "done".to_string(), reasoning: String::new(), run_id: "test-run".to_string(), session_id: "test-session".to_string(), iterations: 1, tool_calls: vec![], error: None } },
```

改为：

```rust
AgentStreamEvent::AgentComplete { response: AgentResponse { content: "done".to_string(), reasoning: vec![], run_id: "test-run".to_string(), session_id: "test-session".to_string(), iterations: 1, tool_calls: vec![], error: None } },
```

- [ ] **Step 3: 更新 caching.rs 测试中的 AgentResponse 构造**

类似更新 `reasoning: vec![]`

- [ ] **Step 4: 运行单元测试**

Run: `cargo test -p vol-llm-agent --lib`
Expected: All tests pass

- [ ] **Step 5: 提交**

```bash
git add crates/vol-llm-agent/src/react/run_context.rs crates/vol-llm-agent/src/observability/plugin.rs crates/vol-llm-agent/src/plugins/caching.rs
git commit -m "test: add RunContext state method tests and update AgentResponse construction"
```

---

## Task 9: 更新 lib.rs 和 PluginContext

**Files:**
- Modify: `crates/vol-llm-agent/src/react/run_context.rs` (PluginContext)

- [ ] **Step 1: 更新 PluginContext::from_run_ctx()**

确保 `PluginContext` 不包含新增的状态字段（这些是内部状态，插件不需要直接访问）。

`PluginContext` 保持不变，因为它只包含插件需要的只读数据。

- [ ] **Step 2: 验证编译**

Run: `cargo check -p vol-llm-agent`
Expected: Compiles successfully

- [ ] **Step 3: 提交**

```bash
git add crates/vol-llm-agent/src/react/run_context.rs
git commit -m "chore: ensure PluginContext does not expose internal state fields"
```

---

## Task 10: 验证和清理

**Files:**
- All modified files

- [ ] **Step 1: 运行完整测试套件**

Run: `cargo test -p vol-llm-agent`
Expected: All tests pass

- [ ] **Step 2: 运行工作空间测试**

Run: `cargo test --workspace`
Expected: All tests pass (vol-llm-agents tests may need updates if they use reasoning field)

- [ ] **Step 3: 检查编译警告**

Run: `cargo check --workspace`
Expected: No errors, warnings are acceptable if pre-existing

- [ ] **Step 4: 提交最终清理**

```bash
git commit -am "chore: final cleanup and verification"
```

---

## 验证命令

实施完成后，运行以下命令验证：

```bash
# 编译检查
cargo check -p vol-llm-agent
cargo check --workspace

# 单元测试
cargo test -p vol-llm-agent --lib

# 完整测试
cargo test --workspace
```

---

## 潜在问题

| 问题 | 缓解措施 |
|------|----------|
| `finalize()` 中 `block_on` 可能导致 panic | 确保只在 agent loop 完成后调用，此时没有运行时在阻塞 |
| 测试中构造 `AgentResponse` 的地方很多 | 使用 IDE 全局搜索 `AgentResponse {` 快速定位 |
| `reasoning` 字段类型变更影响外部代码 | 检查 `vol-llm-agents` 中是否有代码使用该字段 |

---

## 成功标准

1. ✅ 所有编译通过无错误
2. ✅ 所有测试通过
3. ✅ `AgentResponse.reasoning` 是 `Vec<ReasoningStep>` 类型
4. ✅ `RunContext` 包含状态收集字段和方法
5. ✅ `agent.rs` 使用 `RunContext` 方法记录状态
6. ✅ `agent.rs` 调用 `run_ctx.finalize()` 构造响应
