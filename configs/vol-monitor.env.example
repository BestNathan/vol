# vol-monitor.env.example — Environment variables for vol-monitor (Deribit pipeline)
#
# Usage:
#   1. cp configs/vol-monitor.env.example .env
#   2. Fill in actual values
#   3. source .env && cargo run -p vol-monitor
#
# IMPORTANT: Never commit .env file to git!

# =============================================================================
# Deribit API Credentials
# =============================================================================
DERIBIT_CLIENT_ID="your-deribit-client-id"
DERIBIT_CLIENT_SECRET="your-deribit-client-secret"
DERIBIT_WS_URL="wss://www.deribit.com/ws/api/v2"

# =============================================================================
# Feishu/Lark Credentials
# =============================================================================
FEISHU_APP_ID="your-feishu-app-id"
FEISHU_APP_SECRET="your-feishu-app-secret"
FEISHU_RECEIVE_ID="your-feishu-receive-id"

# =============================================================================
# Proxy Configuration
# =============================================================================
HTTPS_PROXY="http://192.168.2.98:8890"
HTTP_PROXY="http://192.168.2.98:8890"
NO_PROXY="localhost,127.0.0.1,192.168.0.0/16,10.0.0.0/8,kubernetes.default.svc"

# =============================================================================
# Logging
# =============================================================================
RUST_LOG="info"

# =============================================================================
# OpenTelemetry/Jaeger
# =============================================================================
OTEL_ENDPOINT="http://localhost:4317"
OTEL_SERVICE_NAME="vol-monitor-dev"
OTEL_SERVICE_NAMESPACE="deribit-dev"
OTEL_DEPLOYMENT_ENVIRONMENT="development"
OTEL_SAMPLE_RATE="1.0"

# =============================================================================
# Config file path (passed via --config or VOL_MONITOR_CONFIG)
# =============================================================================
# VOL_MONITOR_CONFIG="./configs/vol-monitor.example.toml"
