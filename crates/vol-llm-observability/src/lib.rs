//! vol-llm-observability: DEPRECATED stub.
//!
//! All functionality has been consolidated into the `vol-observability` crate
//! (LoggingPlugin, MetricsPlugin, otel_init). RunLogPlugin moved to
//! `vol-llm-agent::run_log_plugin`. This crate is retained only as an empty
//! shell so the workspace loads until downstream references are updated and
//! this crate is removed.
