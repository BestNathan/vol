[package]
name = "vol-tracing"
version.workspace = true
edition.workspace = true


[lints]
workspace = true

[dependencies]
# Tracing with attributes support for #[instrument] macro
tracing = { workspace = true, features = ["attributes"] }
# UUID for generating standard trace IDs
uuid = { version = "1.0", features = ["v4"] }
